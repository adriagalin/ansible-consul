---
galaxy_info:
  author: Matthew Finlayson
  description: Installs and configures consul
  company: Jive Software
  platforms:
    - name: Ubuntu
      versions:
        - All
  categories:
    - system
dependencies:
  - { role: franklinkim.nginx, when: consul_is_ui == true and consul_enable_nginx_config == true }
